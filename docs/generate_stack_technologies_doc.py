# -*- coding: utf-8 -*-
"""Отдельный документ «Стек технологий проекта» (Word). Не изменяет пояснительную записку."""
from __future__ import annotations

from pathlib import Path

from docx import Document
from docx.enum.text import WD_LINE_SPACING
from docx.shared import Pt


def _p(doc: Document, text: str, bold: bool = False) -> None:
    p = doc.add_paragraph()
    run = p.add_run(text)
    if bold:
        run.bold = True
    run.font.size = Pt(12)
    p.paragraph_format.line_spacing_rule = WD_LINE_SPACING.ONE_POINT_FIVE
    p.paragraph_format.space_after = Pt(6)


def build() -> Document:
    doc = Document()
    s = doc.sections[0]
    s.top_margin = s.right_margin
    s.bottom_margin = s.right_margin

    t = doc.add_paragraph()
    t.alignment = 1
    r = t.add_run("Стек технологий учебного проекта")
    r.bold = True
    r.font.size = Pt(14)
    t2 = doc.add_paragraph()
    t2.alignment = 1
    r2 = t2.add_run(
        "«Список задач (TODO)» — fullstack: backend (Python / FastAPI), "
        "веб-клиент (HTML/CSS/JS), клиент Flutter"
    )
    r2.font.size = Pt(12)

    doc.add_paragraph()

    _p(
        doc,
        "Настоящий документ подробно описывает средства разработки и библиотеки, применённые в репозитории "
        "учебного приложения. Исходный код расположен в каталогах backend (серверная часть REST API), "
        "web_client (браузерный клиент без системы сборки) и mobile/todo_app (приложение на Flutter для "
        "мобильных платформ, веба и десктопа). Таблицы в документе не используются; сведения изложены связным текстом.",
    )

    h = doc.add_heading("1. Серверная часть: язык Python, FastAPI и Uvicorn", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Серверная реализация выполнена на языке Python версии не ниже 3.11. Язык выбран за счёт ясного синтаксиса, "
        "широкой поддержки асинхронного программирования и богатой экосистемы пакетов для веб-разработки и работы с данными.",
    )
    _p(
        doc,
        "В качестве веб-фреймворка используется FastAPI: приложение соответствует стандарту ASGI, поддерживает аннотации "
        "типов в обработчиках, автоматически формирует интерактивную документацию OpenAPI (Swagger UI) и применяет асинхронные "
        "обработчики там, где это уместно для ввода-вывода.",
    )
    _p(
        doc,
        "Приложение запускается под сервером Uvicorn (в зависимостях указан пакет uvicorn с набором standard). Uvicorn "
        "реализует протокол ASGI, обслуживает HTTP-запросы к приложению FastAPI и при разработке поддерживает режим "
        "перезагрузки кода при изменении файлов.",
    )

    h = doc.add_heading("2. Контракты данных и конфигурация: Pydantic и pydantic-settings", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Входящие тела запросов и ответы API описываются моделями Pydantic второй версии (в проекте подключены пакеты "
        "pydantic с расширением для проверки email-адресов и pydantic-settings). Это обеспечивает автоматическую валидацию "
        "типов, ограничений полей и понятные сообщения об ошибках клиенту при некорректных данных.",
    )
    _p(
        doc,
        "Параметры окружения (строка подключения к базе данных, секретный ключ для подписи JWT, флаги автосоздания таблиц "
        "и прочее) загружаются через pydantic-settings из переменных среды и при необходимости из файла .env, что соответствует "
        "подходу двенадцатифакторных приложений и упрощает перенос между машинами разработчика и контейнерами.",
    )

    h = doc.add_heading("3. Персистентность: SQLAlchemy 2, драйверы БД и Alembic", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Доступ к реляционной базе реализован через SQLAlchemy версии 2.x в асинхронном режиме (пакет sqlalchemy с "
        "поддержкой asyncio). Сущности «пользователь» и «задача» отображаются на таблицы ORM-моделями; запросы формируются "
        "через современный API select, insert, update и delete без устаревшего стиля Query.",
    )
    _p(
        doc,
        "Для локальной разработки с файлом SQLite используется асинхронный драйвер aiosqlite. Для PostgreSQL применяется "
        "драйвер asyncpg, обеспечивающий неблокирующее взаимодействие с сетевой СУБД. Пакет greenlet входит в зависимости как "
        "часть инфраструктуры асинхронного слоя SQLAlchemy.",
    )
    _p(
        doc,
        "Изменения схемы базы данных версионируются инструментом Alembic: в репозитории хранятся файлы ревизий; команда "
        "upgrade head применяет накопленные миграции к выбранной базе. Это отделяет эволюцию таблиц от прикладного кода и "
        "упрощает согласованное обновление среды разработки и production-подобных установок.",
    )

    h = doc.add_heading("4. Безопасность: bcrypt, JWT (python-jose) и разбор форм", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Пароли пользователей не сохраняются в открытом виде: при регистрации для строки пароля вычисляется стойкий хэш "
        "с использованием библиотеки bcrypt.",
    )
    _p(
        doc,
        "После проверки учётных данных при входе клиенту выдаётся JSON Web Token. Подпись и проверка токена выполняются "
        "средствами python-jose с криптографическим бэкендом (пакет cryptography передаётся как зависимость). В полезной "
        "нагрузке токена размещаются идентификатор пользователя и роль; защищённые маршруты извлекают токен из заголовка "
        "Authorization и отклоняют запрос при истечении срока или неверной подписи.",
    )
    _p(
        doc,
        "Эндпоинт входа принимает данные в формате application/x-www-form-urlencoded; для корректного разбора таких тел в "
        "состав зависимостей включён пакет python-multipart, что является распространённым требованием при использовании "
        "FastAPI с формами OAuth2.",
    )

    h = doc.add_heading("5. Тестирование и качество кода: pytest, httpx и Ruff", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Автоматические тесты серверной части построены на фреймворке pytest с плагином pytest-asyncio для асинхронных "
        "сценариев. Проверяются маршруты через встроенный Test клиент Starlette и FastAPI, в том числе полный цикл операций "
        "над задачами и ограничения доступа для разных ролей.",
    )
    _p(
        doc,
        "Библиотека httpx используется как современный HTTP-клиент в тестах и вспомогательных сценариях, где требуется "
        "явный сетевой вызов.",
    )
    _p(
        doc,
        "Статический анализ и единообразие стиля Python-кода обеспечивает Ruff. В файле pyproject.toml заданы наборы правил "
        "E, F, I, UP и B, охватывающие синтаксис, импорты, обновления под актуальные версии Python и ряд безопасных "
        "улучшений поведения кода.",
    )

    h = doc.add_heading("6. Наблюдаемость: Prometheus и опционально Logfire", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Эндпоинт /metrics отдаёт метрики в текстовом формате, совместимом с системой мониторинга Prometheus, с использованием "
        "клиентской библиотеки prometheus-client.",
    )
    _p(
        doc,
        "При указании токена в переменных окружения может быть включена интеграция с сервисом Logfire (пакет logfire в "
        "зависимостях backend) для централизованного просмотра трасс и ошибок вне локальной машины.",
    )

    h = doc.add_heading("7. Веб-клиент: HTML5, CSS, JavaScript и Font Awesome", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Браузерный клиент представляет собой один файл index.html со встроенными таблицами стилей и сценариями на чистом "
        "JavaScript без транспиляции и сборщика модулей.",
    )
    _p(
        doc,
        "Обмен данными с API выполняется через стандартный интерфейс fetch. Токен доступа и идентификатор пользователя "
        "сохраняются в localStorage. Вёрстка опирается на семантические элементы HTML5; оформление задаётся CSS с "
        "использованием переменных для цветовой схемы и отступов. Пиктограммы подключаются с CDN библиотеки Font Awesome.",
    )

    h = doc.add_heading("8. Клиент Flutter: SDK, сеть, хранилище и локализация", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "Мобильное и настольное приложение создаётся на платформе Flutter с языком Dart (в pubspec.yaml указано ограничение "
        "SDK не ниже версии 3.5). Используется набор виджетов Material Design.",
    )
    _p(
        doc,
        "Для интерфейса на русском языке подключён пакет flutter_localizations вместе с базовым пакетом flutter из SDK.",
    )
    _p(
        doc,
        "HTTP-запросы к REST API выполняются пакетом http. Учётные данные и адрес сервера API сохраняются с помощью "
        "flutter_secure_storage, который на мобильных платформах опирается на системные защищённые хранилища.",
    )
    _p(
        doc,
        "Форматирование дат и времени в списке задач выполняется пакетом intl.",
    )
    _p(
        doc,
        "В качестве набора правил для статического анализа Dart-кода используется flutter_lints из dev_dependencies. "
        "Базовые автоматические тесты интерфейса опираются на пакет flutter_test из SDK.",
    )

    h = doc.add_heading("9. Системы управления базами данных в проекте", level=1)
    h.runs[0].font.size = Pt(14)
    _p(
        doc,
        "В типовом сценарии разработки используется встроенная СУБД SQLite: база хранится в одном файле на диске, строка "
        "подключения задаётся переменной DATABASE_URL в файле окружения.",
    )
    _p(
        doc,
        "Для приближения к условиям эксплуатации предусмотрен запуск PostgreSQL через compose-файл в корне репозитория; "
        "приложение подключается к ней асинхронным драйвером asyncpg после применения миграций Alembic.",
    )

    _p(
        doc,
        "Перечень версий пакетов Python зафиксирован в файле backend/requirements.txt; зависимости Flutter перечислены "
        "в mobile/todo_app/pubspec.yaml. При расхождении с настоящим текстом приоритет имеют эти файлы.",
        bold=True,
    )

    return doc


def main() -> None:
    out = Path(__file__).resolve().parent / "STACK_TEKHNOLOGIY_PROEKTA.docx"
    build().save(out)
    print(f"Written: {out}")


if __name__ == "__main__":
    main()
